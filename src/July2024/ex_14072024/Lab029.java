package July2024.ex_14072024;

public class Lab029 {
    public static void main(String[] args) {

        // Operator
        // data_type variable_name(identifier) = variable_value(literals)

           //Unary Operator -> only 1 operand, operator

        int age = 65;
        int lambo = -1;

        //Unary = only 1 operand, operator
        // age -> operand, value -> Unary plus, = -> operator
        // lambo -> operand, value -> Unary minus, = -> operator

        // Binary Operator -> Two

        int a = 10;
        int b = 23;
        int sum = a+b;
        // Binary -> arithmetic +, -, *, /
        // a, b -> operand, + -> operator

        // Ternay Operator -> Three or more Operand
        // a,b,c -> Operand

    }
}
